<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>net9.0;net10.0</TargetFrameworks>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>LionFire</RootNamespace>
    <Configurations>Debug;Release</Configurations>
    <Description>Asynchronous data access patterns with support for caching, lazy loading, polling, and observable operations.</Description>
    <PackageTags>async data abstractions observable</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\LionFire.Resolves.Abstractions\LionFire.Data.Abstractions.csproj" />
  </ItemGroup>

	<ItemGroup>
		<PackageReference Include="MorseCode.ITask" />
		<PackageReference Include="System.Reactive" />
	</ItemGroup>
	
</Project>
