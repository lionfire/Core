#nullable enable
using System;
using System.Collections.Generic;
using System.Text;

namespace LionFire.ExtensionMethods
{
    public static class IDictionaryExtensionsNullable
    {
        //public static TValue? TryGetValue2<TKey, TValue>(this IDictionary<TKey, TValue> dict, TKey key, TValue defaultValue = default(TValue))
        //    where TValue : class
        //{
        //    if (key == null) return default;
        //    if (!dict.ContainsKey(key)) return defaultValue;
        //    return dict[key];
        //}

    }
}
