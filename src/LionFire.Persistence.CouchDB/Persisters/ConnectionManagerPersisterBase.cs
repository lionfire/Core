#if IDEA

// Idea: named connections from a ConnectionManager

namespace LionFire.Persistence.CouchDB
{
    public class ConnectionManagerPersisterBase<TOptions, TConnectionManager>
        where TConnectionManager : IConnectionManager
    {

    }
}
#endif