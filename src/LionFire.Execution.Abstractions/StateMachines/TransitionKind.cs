namespace LionFire.Execution
{
    // UNUSED FUTURE: descriptor attributes, for more complex Transition enum types
    public enum TransitionKind
    {
        Unspecified = 0,
        Initialize,
        Run,
        Wait,
        Cleanup,
    }
}
